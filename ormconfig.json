{
  "username": "guilherme",
  "password": "password",

  "name": "default",
  "type": "postgres",
  "host": "localhost",
  "port": 5432,
  "database": "unit-tests",
  "entities": [
    "./src/modules/**/entities/*.ts"
  ],
  "migrations": [
    "./src/database/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/database/migrations"
  }
}
